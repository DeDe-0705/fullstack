# Server (NestJS)

基于 [NestJS](https://nestjs.com) 的后端服务。

## 快速开始

```bash
pnpm dev       # 开发模式（热重载）
pnpm build     # 构建
pnpm start     # 生产模式启动
```

服务默认运行在 [http://localhost:3000](http://localhost:3000)。

## API 端点

- `GET /api/health` — 健康检查
