# 2026-08-02 工作日志

## 面试复习
- 展开补充了前端工程化.md 第五章 Monorepo：
  - 5.1 Monorepo vs Multirepo 对比
  - 5.2 为什么需要 Monorepo（三大痛点）
  - 5.3 pnpm workspace + Turborepo 工具链
  - 5.4 Turborepo 缓存原理
  - 5.5 实际项目结构（结合 Li People 场景）
  - 5.6 面试话术
- 补充了前端工程化.md 3.3.5 节 Tag vs Commit 区别：
  - 本质区别（动态历史 vs 静态锚点）
  - 运用场景对比表
  - 为什么发布用 Tag 不用 SHA
- 补充了前端工程化.md 3.3.6 节 Git Tag 常见问题：
  - git tag 默认打在本地 HEAD（和远程无关）
  - Tag 重名处理（先删再建，远程要 force）
  - git push 默认不带 tag，--follow-tags 只推关联附注标签
- 创建了 interview-prep/projects/ 目录，完成 5 个项目面试要点文档：
  - li-people/README.md：微前端架构、四大核心能力、G6 组织架构图
  - lixiang-tongshi/README.md：SSE 流式渲染、动态组件 CDN、Iframe 通信
  - churu-yuyue/README.md：Claude Code Skill、Sub-Agent 协同、AI 驱动研发
  - zuzhi-xinxi/README.md：G6 可视化、Taro 双端、权限管控、无限下钻
  - lixiang-wenhua/README.md：瀑布流、大文件分片上传、视频组件封装
  - projects/README.md：总览、面试策略、差异化优势、复习建议

## Li People 文档多轮修正（用户对真实项目细节的校准）
- 背景修正：各业务是**各自独立系统、不同代码库**（非耦合在一个库）
- 鉴权修正：独立 `@li-people/auth-sdk`，NPM + CDN 双渠道（老项目 script 引入），不走 Props 注入
- 依赖共享修正：jsBeforeLoaders 注入 lodash/axios 到子应用 window + external 构建
- 预加载修正：requestIdleCallback 空闲调度 + 三层机制（资源预加载→exec 预执行→激活）+ Fiber 加速 + 包分片
- 保活修正：真正 KeepAlive（不 unmount，DOM/状态/定时器全保留）vs 单例模式（复用 iframe 但重新 mount）
- 沙箱修正：WebComponent + Proxy 代理 DOM API（JS 在 iframe 执行、DOM 在父页面渲染）
- 路由修正：独立 history + 代理 pushState + 查询参数编码，sync:true 自动恢复无需监听事件
- **Props 传递机制修正（重要）**：Props 不走 postMessage/JSON 序列化，而是利用同源 iframe 的 contentWindow 访问能力，把运行时对象挂载到子应用 `window.__WUJIE`（含 props/bus/路由信息），引用传递，函数/Date/Map/Set 都能直接传；只有持久化（localStorage/URL）场景才需要序列化
- 选型理念（用户原话）："没有最好的微前端方案，只有最适合业务的方案"
- 新增文档：wujie-core-mechanism.md（三层设计+Proxy 代理实现）、wujie-proxy-pitfalls.md（vxe-table 踩坑）
- 精简了「能力三：通信 — Props + Bus 双通道」章节（约 300 行 → 110 行）：
  - 合并"Props 底层机制"和"能传复杂对象吗"两个追问为一个 QA
  - 删除冗余示意图、WebSocket/DOM/响应式对象三个长代码示例（压缩为核心片段）
  - 表格从 9 行压到 4 行，保留全部核心 QA
- 重构精简了「能力四：性能 — CDN + 依赖共享 + 预加载 + 保活」章节（约 540 行 → 200 行）：
  - 按 1.依赖共享 / 2.预加载 / 3.保活 三个子章节重新组织，修复了孤立的"4."编号问题
  - 删除冗余：jQuery 老项目 HTML 示例、Fiber workLoop 实现、保活/单例两个 class 底层实现、LRU 实现、三套预加载策略文本
  - 合并重复 QA：保活内存泄漏并入保活的坑、Fiber 优势并入三层机制 QA、公共库更新并入 jsBeforeLoaders QA
  - 保活 vs 单例表格从 11 行压到 7 行
- 按用户反馈二次重构「能力四」：
  - 标题改为「性能 — 预加载 + 预执行 + 保活」，问题描述聚焦真实痛点（首次进入慢白屏 1-2s + 切换重新挂载状态丢失），不再以依赖共享为引子
  - 子章节顺序调整为 1.预加载（核心）→ 2.保活 → 3.依赖共享（辅助手段，减小体积）
  - 新增总览面试话术（两个场景：首次进入靠预加载、再次切换靠保活）
  - 性能数据改为预加载/保活优先表述

## 组织信息管理平台文档修正（zuzhi-xinxi/README.md）
- PC 端 UI：Element Plus → 公司组件库（基于 Element Plus 二次开发），共 2 处
- G6 章节修正（联网核实）：
  - G6 TreeGraph **没有虚拟渲染能力**，删除"虚拟渲染/布局缓存/防抖"等编造的优化点
  - 懒加载正确 API：`graph.findDataById(id)` → `parentData.children = childData` → `graph.changeData()`（之前写的 `graph.updateChild()` 是错的）
  - 方案描述对齐用户真实实现：初始展示二层结构，点击节点才请求下层数据追加到图中
  - 代码大幅精简，去掉 G6 配置细节堆砌
- 3.2 联动组件：去掉 120 行模板代码，改为方案讲解——人员详情组件 + 组织详情组件互相递归调用 + 导航栈，新增"为什么不用一个组件 if-else"追问
- 3.3 权限管控：去掉 110 行代码（DataScope/canViewEmployee 均为编造），改为方案——依赖 Li People 统一权限管理，后端控数据范围和操作校验（底线），前端控页面/按钮显隐（体验）
- 4.2 NutUI 二次封装：去掉代码，真实方案是统一修改样式变量对齐公司设计规范，不重写组件
- 删除"跨端组件复用"（用户确认不存在）：

## 理想同事文档修正（lixiang-tongshi/README.md）
- 删除 4.2 多端兼容方案（不存在，isMobile/batchSize 为编造）+ 亮点表多端兼容行
- 3.4 Iframe 通信重写：真实场景是宿主系统把整个表单对象+校验报错通过 postMessage 传给 AI 分析，AI 分析后结构化回填表单（FORM_CONTEXT/FORM_FILL 双向闭环）；删除编造的 CONTEXT_SYNC/场景问候协议
- 3.3 动态组件加载修正：AI 返回组件 CDN url → script 标签加载 → 组件 JS 执行后内部 app.component() 全局注册 → 直接用组件名 <component :is="'leave-form'"> 渲染；删除 manifest 配置中心和动态 import 方案
  - 二次修正：一个脚本包含多个表单组件，加载后批量注册；包名 LeaveForm → BizForms（包级）
  - 新增"为什么 UMD 不是 ESM"QA：script 直载无打包器解析裸标识符，UMD 加载即执行 + globals 取全局 Vue，自闭合兼容性好
  - 三次修正：动机对齐"快速发版迎合多变需求、主应用不发版"；整节精简（删流程图、合并 QA、压缩代码）
- 3.2 流式渲染重写：真实方案是 markdown-it（插件生态 + token 流拦截自定义，代码块交 highlight.js）；删除编造的 rAF 批量渲染/增量解析/未闭合标签检测代码（用户确认未做性能和闭合处理）；未闭合话术改为"markdown-it 容错按纯文本展示、全量重渲染自然正常，现阶段无动力复杂化"
- 3.2 二次修正（用户记错）：真实渲染库是 **markstream-vue**，四个选型理由：① 流式碎片解析极强（thinking/form 标签半截不闪烁）② 自定义 HTML 标签映射 Vue 业务组件（请假卡片/个人信息卡片/政策弹窗）③ 作用域隔离（多对话窗口/子应用嵌套）④ 体积轻内网打包友好；QA 全部对齐 markstream-vue
- 3.2 三次修正（用户指出两条链路混淆）：自定义标签对应的组件**不是 markstream-vue :components 映射本地组件**，而是走 3.3 CDN 链路——script 加载后全局注册，markstream-vue 按标签名交给 Vue 命中全局组件；3.2 和 3.3 是一条完整链路的两端
- 3.2 四次修正（重要）：AI **不返回 HTML 标签**，而是在 JSON 中下发组件指令（type:'component' + 组件名 + CDN url + props），前端按指令 script 加载后用 `<component :is>` 渲染；markstream-vue 只负责文本流式渲染
- 3.2 五次修正：markstream-vue 支持自定义 code/thinking 标签渲染，用户做了处理——code → 高亮+复制按钮代码块，thinking → 可折叠思考过程卡片（slot 插槽形式）
  - 删除 4.1 跨端组件复用整节（useEmployee Composable 代码为编造）
  - 删除 Q1"PC 和移动端如何复用代码"高频题（含编造的"复用率 60%"），Q 编号顺延
  - 第四章改为"移动端定制（Taro + NutUI）"
  - 一句话介绍/技术挑战/亮点表中所有"跨端复用、60% 复用率"表述清除
- 3.2 六次修正（用户质疑 slot 用法，要求查官方文档）：WebSearch 核实 markstream-vue 官方文档（markstream.simonhe.me/guide/custom-components），**slot 不是官方 API**。正确方式：
  - `createApp(App).use(VueRendererMarkdown, { components: { thinking: ThinkingBlock } })` 标签→组件映射注册
  - 渲染侧 `<MarkdownRender :custom-html-tags="['thinking']" custom-id="chat" />` 白名单声明
  - 自定义组件 props 为 `{ type, tag, content, raw, attrs, loading, autoClosed }`，loading 表示流式中间态
  - code 高亮走保留的内置 override key `code_block`（node renderer override，非自定义标签）
  - 文档已全部改为官方 API 写法（components 映射 + custom-html-tags），删除 slot 代码
- 3.2 七次修正（用户确认实际用法）：注册用 `VueRendererMarkdown` 插件形式；code 块是 highlight.js + 自定义组件（覆盖内置 code_block key）。重写自定义标签代码块：thinking → ThinkingBlock 映射 + custom-html-tags 白名单；code_block → CodeBlock 组件（hljs 高亮+复制按钮）。清除"不是通过 slot"等历史包袱表述，全篇校对无残留
- 3.2 八次修正（用户要求去代码）：自定义标签处理整段代码删除，只留一段文字说明——"markstream-vue 支持把标签映射成自定义 Vue 组件，thinking → 可折叠思考卡片（流式中间态展示），代码块 → 自定义组件覆盖默认渲染做高亮+复制"；选型理由第2条同步精简。用户观点：面试官不需要知道 API 细节，知道支持这么做即可
