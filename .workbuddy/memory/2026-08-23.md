# 2026-08-23 工作日志

## Vue3 模拟面试（第 9、10 轮）+ 全天知识点陪练

### 模拟面试第 10 轮：Vue3 原理复测（同日归档 2026-08-23.md）
- 总分 76/100（第 9 轮 55 → 76，**复测通过**）。5 道变体题全考上午空白点
- 攻克：PatchFlag/BlockTree（☆→75%）、惰性代理（85%）、computed 两层短路（90%）
- 未过关：onCleanup 竞态手写（55%）——概念通、代码写不出
- 新知识遗留：最近 block 原则（动态节点只进最近的 block，v-if/v-for 截断向上收集）、proxyMap 两个短路、markRaw 拼写（已错两次）
- 遗留行动项：两个竞态模板默写（AbortController 版 + token 版）、完整更新链路背诵（set→trigger→effect队列→vnode→patch→DOM）
- **下一步：进 React 专场（Fiber 架构 + Hooks 原理开考）**

### 模拟面试第 9 轮：Vue3 专题（归档 mock-interviews/2026-08-23.md）
- 总分 55/100，历轮最低。7 问覆盖：Proxy 局限与补救、惰性代理、依赖收集三层结构、computed 两层短路、PatchFlag/BlockTree、watch onCleanup 竞态、defineModel 链路
- **核心发现**：API 使用层扎实（4 年实战），但原理"时序层"成片空白——惰性代理、两层短路、onCleanup、PatchFlag/BlockTree 全部答不出。"知道名词"和"讲出机制"之间存在断层
- **最大盲区**：PatchFlag/BlockTree/静态提升（简历写了 Vue3 深度，最大面试风险点）
- 术语新增三个混淆点：劫持≠依赖收集、effect≠DOM元素、WeakMap 回收的是数据对象非视图元素
- 文档更新：Vue3深度原理.md 补了 computed 两层短路（1.5 节）+ 2026-08-23 盲区清单（8 条，含 PatchFlag 30 秒极简记忆版）+ toRaw/markRaw/proxyMap 详解（1.7 节，含 ECharts 实例事故现场）
- 追加陪练：toRaw（只读逃生门/只脱一层/改raw不触发）、markRaw（__v_skip、子树全免代理、ECharts/G6 送命题）、proxyMap（全局唯一代理、reactive(reactive(x)) 短路）
- 下一步：48 小时内重读 Vue3深度原理.md 第一、二章，之后安排"Vue3 原理复测"，通过后进 React 专场

### 全天知识点陪练（未归档的散题）
- CommonJS 值拷贝 vs ESM 只读引用（live binding）
- src vs href 语义与阻塞行为差异
- 大文件分片上传链路：Blob.slice 是字节级惰性视图（零拷贝）、断点续传的 hash 是前端算好上报（内容寻址）、后端靠分片计数判断完成、File 修改与 NotReadableError
- SPA vs SSR（含 Hydration 水合）
- JWT 三段结构与双 Token 方案的缺点（并发刷新竞态、半有状态、存储分离）
- 虚拟滚动定高方案（公式、buffer、translateY 原理）
- wujie props 注入：同域 iframe 直接引用赋值、Date/File/函数可传、跨 realm instanceof 失效
- HTML5 新特性六大类、customRef 经典用法（防抖 ref / 存储同步 / track-trigger 自定义）
