# 2026-08-03 工作日志

## 理想文化项目文档修正（lixiang-wenhua/README.md）
- 删除"视频上传成功率 99.9%"（编造数据）和"多端适配/一套代码兼容 PC 和移动端"表述（与 zuzhi-xinxi 项目口径冲突，用户确认不存在跨端复用）
- 删除项目亮点总结中"多端适配"行
- 待确认：瀑布流/大文件上传/视频播放组件三大能力真实性、组件库混用问题（el-upload vs nut-icon）、项目时间

## 理想文化项目文档修正（续）
- 3.1 瀑布流：用户确认 UGC 列表主要是图片，不存在视频尺寸不一。删除视频封面/播放图标/时长等视频相关代码，改为纯图片瀑布流；业务定位改为"图片分享平台"；一句话介绍补充"图片/视频"
- 3.1 瀑布流二次修正：真实方案是**固定两种比例**（4:3 横图、3:4 竖图），图片取中间区域裁剪（`object-fit: cover`），不是动态计算高度。删除 `grid-auto-rows`/`getItemSpan`/`onImageLoad` 等动态计算代码，改为纯 CSS 固定比例方案；高频题 Q1 同步改为"为什么选固定比例"；亮点表同步更新
- 3.1 瀑布流三次修正：真实方案是 **Absolute 定位 + 动态计算位置 + 移动动画过渡**——维护 `columnHeights` 数组记录每列高度，新 item 放最短列，计算 x/y 坐标；`position: absolute` + `transform: translate(x, y)` 定位；`transition: transform` 实现位置变化平滑动画。删除 CSS Grid 方案，改为 Absolute 定位；面试追问改为"为什么不用 CSS Grid/Flexbox"（强调动画需求）；高频题 Q1 同步改为"为什么选 Absolute 定位"
- 四、多端适配方案修正：用户确认主要针对 UGC 展示，视频播放配置保持一致。删除 SCSS 媒体查询（与 Absolute 定位方案冲突），改为 JS 动态计算列数（PC 4列/平板 3列/移动端 2列）；视频配置改为多端统一（不自动播放、内联播放）；删除"移动端禁用自动播放"等平台差异配置
- 组件库修正：用户确认 PC 端用公司组件库（基于 Element Plus 封装），移动端用 Vant。删除 NutUI（nut-icon/nut-popup），统一改为公司组件库（company-icon/company-popup/company-upload/company-button/company-progress）；ElMessage 改为公司组件库的 Message；文档头部补充组件库说明
- 历史包袱清理：删除"JS 计算每个 item 的 `top/left`"（实际是 transform）；CSS 类名 `.nut-icon` → `.company-icon`
- 四、多端适配方案二次修正：既然视频播放配置统一，就删除 `videoConfig` 代码块和"视频播放配置统一"适配要点；删除 `isMobile`/`waterfallColumns` 等未使用的工具函数；开头改为"主要针对 UGC 内容列表的展示"
- 五、性能优化新增：用户确认 UGC 做了图片渐进式加载——先加载低清晰度占位图，再用 `data-src` 加载高清图，根据元素可见性（IntersectionObserver）做替换。新增 5.1 节，包含核心逻辑（低清晰度占位/可见性检测/预加载替换/过渡动画）和面试追问（为什么不用原生懒加载）；亮点表新增"图片渐进式加载"行；章节编号顺延（五→六、六→七）
- 修复重复：第五章性能优化因编辑失误写重复了（两份完全相同的 5.1），删除重复的一份；章节结构确认：一介绍/二背景/三核心能力/四多端适配/五性能优化/六高频题/七亮点
- 3.1 瀑布流统一渐进式加载口径：代码中 `loading="lazy"` → `:src="item.lowQualityCover" :data-src="item.cover"` + IntersectionObserver 渐进式替换；核心逻辑新增第 5 条渐进式加载（引用 5.1）；代码精简（删除空函数/滚动加载更多代码/冗余样式，标注"精简示意"）
- 3.2 大文件上传重写：真实方案是**基于百度云 BOS 对象存储 SDK**——删除全部自编 ChunkUploader 代码（150+ 行），改为方案说明（BOS Multipart Upload 内置分片/断点续传/秒传，前端只接入 SDK + 进度展示）；面试追问改为"为什么用 BOS 而不是自己实现"；高频题 Q2 改为"大文件上传是怎么做的"，Q3 改为"如果不用 BOS 自己实现分片上传怎么做"（保留手写知识点作为追问应对）；技术栈和亮点表同步更新
- 3.3 视频播放组件精简：180 行 → 75 行（删除冗余样式/事件处理展开，保留 xgplayer 初始化 + 自定义控制栏核心结构）
- 四、多端适配要点修正："图片懒加载 loading=lazy" → "图片渐进式加载（见 5.1）"，口径统一
- 3.2 大文件上传二次修正（重要）：用户提供 BOS 官方文档（cloud.baidu.com/doc/BOS/s/Ylch3vco8），**SDK 不会自动分片**——真实用法是三段式 API 手动编排：`initiateMultipartUpload`（拿 uploadId）→ `uploadPartFromBlob`（前端自己 file.slice 切片 + 并发控制，逐片上传记录 eTag）→ `completeMultipartUpload`（提交 partList 服务端合并）；断点续传基于 `listParts(uploadId)` 查已传分片；放弃用 `abortMultipartUpload`。删除"SDK 内置自动分片/秒传"错误表述，改为手动三段式 + 精简代码示例；面试追问改为"为什么用 BOS 而不是完全自己实现"；高频题 Q2/Q3 同步重写；亮点表改为"三段式 API + 断点续传"
  - 备注：BOS SDK 有 `putSuperObject` 高级封装可自动分片，但用户确认项目用的是手动三段式，按项目实际为准
- 用户确认：BOS 手动三段式为项目真实方案，文档已对齐。理想文化项目文档基本完成，仅剩项目时间（2025.02-2025.03 两个月）待确认
- 第四章合并：多端适配 + 性能优化（图片渐进式加载）合并为"四、多端适配与性能优化"，删除独立的第五章；3.1 核心逻辑引用改为"详见第四章"；章节编号修正（五高频题/六亮点），最终结构：一介绍/二背景/三核心能力/四多端适配与性能优化/五高频题/六亮点
- 第四章补充：用户要求恢复图片渐进式加载代码——新增 `useProgressiveImage` composable 完整实现（IntersectionObserver + rootMargin 50px + new Image 预加载替换 + blur 过渡）+ 模板/样式用法 + 核心逻辑 4 条；与 3.1 瀑布流代码中的渐进式加载写法口径一致
- 结构调整："多端适配与性能优化"并入第三章核心能力建设，成为 3.4（用户观点：多端适配与性能优化也属于核心能力）；章节编号修正为：一介绍/二背景/三核心能力（3.1瀑布流/3.2 BOS上传/3.3视频播放/3.4多端适配与性能优化）/四高频题/五亮点；3.1 引用改为"详见 3.4"

## 出入预约项目文档修正（churu-yuyue/README.md）
- 头部技术栈：删除 NestJS（后端只有 Spring Boot）、删除 Sub-Agent；改为"Vue3 + TypeScript + Spring Boot + Claude Code"。用户明确：只改头部技术栈描述，正文内容（Skill/Sub-Agent/MCP 流程等）保留不动
- 待确认：效率数据（50%+/联调2天→0.5天）、Sub-Agent 落地真实性、工具链（lark-cli/Playwright MCP）、业务代码（状态机/v-permission）、项目时间 2026.01-2026.04
