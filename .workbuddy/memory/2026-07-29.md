# 2026-07-29

## 前端面试准备 - 知识体系搭建

- 完成身份初始化（德德 / 小Q），基于简历（王德师，理想汽车高级前端，北京求职）建立了用户画像
- 为 interview-prep/ 新增8个模块，覆盖高级前端面试全部核心领域：

### 新增模块
1. **Vue3深度原理.md** - Proxy响应式、虚拟DOM diff/PatchFlags、Compiler优化、Composition API
2. **React核心机制.md** - Fiber架构、Hooks原理、并发模式、Vue/React对比
3. **前端工程化.md** - Webpack/Vite原理、HMR、TreeShaking、组件库建设、Monorepo
4. **性能优化体系.md** - 网络/构建/渲染/运行时四层优化、Core Web Vitals
5. **微前端专题.md** - 无界实战方案（结合理想汽车项目）、主流方案对比
6. **手写代码.md** - 防抖节流、深拷贝、Promise操作、LRU、bind/apply、虚拟列表等
7. **算法基础.md** - 排序、树遍历、链表、DP入门、经典场景题
8. **项目复盘与面试话术.md** - 四大项目STAR话术、系统设计题

- 更新 README.md，添加学习路线建议（分周计划）

## 浏览器缓存模拟面试

- 问题链：缓存层级 → 强/协商缓存 → SW vs HTTP → HTML/JS缓存策略 → 版本热更新 → SW更新机制
- 德德基础扎实：缓存层级正确、能区分 no-cache/no-store、实战策略（HTML no-cache + hash强缓存）理解到位
- 纠正：no-store 是"不缓存"非"走最新缓存"；max-age 优先级高于 Expires
- 补充了版本更新方案：轮询 → SW自动检测 → WebSocket推送 → version.json轻量方案
- 德德深度追问了 SW 更新本质 = sw.js 注入 build hash，触类旁通

## 客户端存储模拟面试

- 问题链：容量/生命周期 → Cookie自动发送 → SameSite防CSRF选型 → storage事件跨Tab → localStorage写满处理 → 同步vs异步
- 德德数据准确（4KB/5MB/5MB/无上限），SameSite理解正确但选Strict偏严（B端用Lax）
- 纠正：localStorage写满抛QuotaExceededError，不会自动LRU；Cookie核心区别要先说"自动发送"再提安全属性

## 网络与HTTP补充

- 更新网络与HTTP.md，新增两个面试核心话题：
  - **SSO单点登录**（五）：共享Cookie/ JWT透传/ CAS中心认证/ 微前端SSO实战、OAuth 2.0/OIDC
  - **CSP内容安全策略**（六）：10+指令、配置方式、Report-Only上线流程、XSS最后防线
## 前端工程化补充 — CI/CD + 组件库 + Git Tag

- 在 前端工程化.md 的 CI/CD 章节新增 3.3 小节，覆盖：
  - **完整流水线**：Lint → Test → Build → 版本决策 → Git Tag → npm publish → CDN 上传 → 通知
  - **版本号决策**：Conventional Commits (fix=patch, feat=minor, BREAKING=major)
  - **Git Tag 四个核心价值**：可追溯、可对比、可回滚、NPM/CDN/Git 三对齐
  - **GitHub Actions 完整配置**：changesets 自动发版 + CDN 上传 + 飞书通知
  - **子应用消费策略**：精确锁定版本、CDN 按版本号分目录、版本隔离
  - **面试话术模板** + 4 个常见踩坑

## SSO + CSP 模拟面试

- 问题链：微前端SSO鉴权 → Token传递方式 → 过期处理架构 → 草稿兜底 → CSP策略编写 → CSP灰线上线
- **SSO**：德德实战经验丰富，双Token + 独立鉴权SDK + 请求拦截器方案完整清晰
- **纠正**：`window.open` → `location.href`（不需要新窗口）；CSP和CORS是两套独立机制（CSP拦截加载、CORS拦截读取）
- **补课**：CSP Report-Only 灰线上线流程（1-2周只上报不拦截 → 分析违规 → 切正式策略）

## 计算机网络模拟面试

- 问题链：HTTP版本对比 → HPACK头部压缩 → TLS握手 → 证书验证 → Charles中间人 → CORS OPTIONS预检 → TCP握手挥手
- **亮点**：TCP全双工挥手（"服务端还有数据没处理完"）和 Charles中间人（"我授权了所以不算"）答到本质
- **盲区**：HPACK原理、TLS证书验证四级关卡、OPTIONS简单vs非简单请求规则 — 已全部回填文档
- 更新 网络与HTTP.md：新增 HPACK字典表机制 + TLS两段式设计+证书验证+Charles原理 + CORS预检触发规则+Access-Control-Max-Age缓存 + TCP挥手细节 + **DNS域名系统** + **CDN内容分发网络**

## DNS与CDN模拟面试 + 文档补充

- 问题链：DNS解析链路 → TTL缓存延迟 → CDN旧缓存排查 → hash版本隔离 → 部署顺序404
- 德德对hash文件名版本隔离理解到位；TTL和部署顺序为盲区
- 文档补充：网络与HTTP.md 新增第八章 DNS与CDN（解析链路、记录类型、优化手段、CDN原理、缓存策略、Li People项目实战架构、面试话术）

## 今日五轮 · 汇总

共29次追问，五轮全覆盖：HTTP缓存 → 客户端存储 → SSO+CSP → 计算机网路 → DNS/CDN
分析已归档至 mock-interviews/2026-07-29.md

## 浏览器原理模拟面试 + 文档补齐

- 问题链：渲染六阶段 → async/defer → CSS阻塞JS → 分层/GPU加速 → 强制同步布局 → 进程/线程
- 德德渲染管线框架扎实（六阶段完整），需补：async精确语义、CSS阻塞JS原因、will-change大坑、强制同步布局
- 浏览器原理.md 新增：async/defer对比表 + CSS阻塞JS详解 + 强制同步布局 + 图层分层/GPU合成 + will-change最佳实践 + Chrome进程/线程架构

## 今日六轮 · 最终汇总

共35次追问，六轮全覆盖，六份知识文档（网络与HTTP / 前端工程化 / 浏览器原理）均完成补全。
归档：mock-interviews/2026-07-29.md（含全部追问+评分+分析+最终画像）

## 今日四轮模拟 · 汇总

共24次追问，覆盖HTTP缓存/客户端存储/SSO+CSP/计算机网络四大方向。

| 模块 | 分 |
|------|-----|
| HTTP缓存 | ⭐⭐⭐⭐ |
| 客户端存储 | ⭐⭐⭐⭐ |
| SSO鉴权 | ⭐⭐⭐⭐⭐ |
| CSP | ⭐⭐ |
| 计算机网络 | ⭐⭐⭐ |

**核心优势**：实战代入感极强，追问能触类旁通。**最大盲区已定性**：不是理解能力问题，是部分协议细节没接触过，补上就是面霸。
